import { Router } from "express";
// import { UserController } from "../controllers/auth/UserController";
export const userRouter = Router();

// const user = new UserController
// userRouter.route('/user').get(user.getAll)